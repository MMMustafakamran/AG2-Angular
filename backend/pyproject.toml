[project]
name = "backend"
version = "0.1.0"
description = "AG2 agent exposed over AG-UI for the CopilotKit Angular harness."
readme = "README.md"
requires-python = ">=3.10,<3.15"
dependencies = [
    "ag2[ag-ui,openai]>=1.0.3,<2",
    "fastapi>=0.128.0",
    "httpx>=0.27.0",
    "python-dotenv>=1.2.2",
    "uvicorn>=0.40.0",
]

[tool.uv]
package = false
